method: llm
label: LLM-guided exact (type-off)
mode: bucketed
filter_prefix: llm_percentile_ehu
score_method: llm
curriculum_method: llm_guided_v4
sampler_mode: exact_bucket_quota
max_repeat_per_scenario: 4
hard_subtype_balance: false
tie_break_mode: stable_hash
bucketization_mode: percentile_tercile
bucket_cardinality_contract: equal_cardinality_quantile
bucket_master_filter: uniform_train_all
phase_names:
  a: easy_warmup
  b: hard_ramp
  c: hard_replay
type_routing:
  supported: true
  default_mode: "off"
  metadata_path: ../llm-taxonomy/artifacts/rationales_ssg_text_v4_0_train_normalized.csv
  enabled_sampler_mode: exact_bucket_quota
exposure:
  # Common source-comparison contract: no LLM-only persistent/group controls.
  persistent: false
  near_duplicate_group_weighting: false
  max_repeat_per_near_duplicate_group: 0
  max_cumulative_exposure_per_scenario: 0
  max_cumulative_exposure_per_near_duplicate_group: 0
